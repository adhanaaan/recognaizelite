// Stub for RecognaizeDemo — voice-over playback is not used in the demo build.

export interface VoiceOverProps {
  source?: string;
  onComplete?: () => void;
}

export function VoiceOver(_props: VoiceOverProps) {
  return null;
}
